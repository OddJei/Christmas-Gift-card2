# Chrstmass-Gift

Static site using style.css.

How to deploy:
1. Initialize a Git repo, commit all files and push to GitHub (branch `main`).
2. Ensure `.github/workflows/deploy.yml` is present (it is included).
3. On push to `main` the workflow will run and publish the site to GitHub Pages automatically.
4. Visit the repository Settings → Pages to see the published URL (if needed).
